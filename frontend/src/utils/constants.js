import App from "App";
import { checkAppType } from "./utils";

export const MonitorType = {
    NONE: "none",
    FACE_MESH: "face_mesh",
    OBJECTRON: "objectron",
    POSE: "pose",
};

export const AppType = {
    HEALTHCARE: "healthcare",
    EDUCATION: "education"
}